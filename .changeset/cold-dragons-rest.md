---
"@himd/voice-bridge": minor
---

Collapse audio pipeline to DashScope only (Qwen Omni + Qwen TTS). Single `DASHSCOPE_API_KEY` for both understanding and synthesis. Removed GLM/Zhipu dependencies, provider factory, and fallback logic. New instructions-based TTS control. Version bump to 0.7.0.
