---
"@himd/voice-bridge": minor
---

Restructured HiMD into a pnpm monorepo with a public MCP package and `packages/voice-bridge/` and a Claude Code plugin in `plugins/himd/`. Added CLI entrypoint (`himd-voice-bridge`), actionable runtime diagnostics, tests, and docs.
