{
  "name": "himd-monorepo",
  "private": true,
  "packageManager": "pnpm@10.8.0",
  "scripts": {
    "check": "pnpm --filter @himd/plugin-himd check && pnpm --filter @himd/voice-bridge test",
    "release:status": "changeset status --verbose",
    "version:apply": "changeset version && node scripts/sync-plugin-manifest-version.mjs"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.5"
  }
}
